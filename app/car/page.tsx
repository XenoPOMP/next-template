import { FC } from 'react';

import Meta from '@/src/components/seo/Meta/Meta';

const CarPage: FC<{}> = () => {
  return (
    <main>
      <Meta title={'Car page'} />
    </main>
  );
};

export default CarPage;
