// @ts-expect-error Cypress has own imports
describe('Main page', () => {
  // @ts-expect-error Cypress has own imports
  it('opens', () => {
    // @ts-expect-error Cypress has own imports
    cy.visit('/');
  });
});
